-- Виджеты Главной удалены из приложения (сам экран «Главная» убран,
-- UI для их добавления никогда не был подключён к навигации) —
-- таблица больше не используется ни одним экраном.
drop table if exists public.home_widgets;
